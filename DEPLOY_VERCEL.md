# Deploy a Vercel

Este archivo ha sido consolidado. Para instrucciones de deploy, consulta el archivo `DEPLOY.md`.

Este archivo se mantiene por compatibilidad con referencias anteriores en el repositorio.

